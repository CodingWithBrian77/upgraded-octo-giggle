x = 3.14
y = 4
z = 5

# rounds a variable to nearest whole integer
# result = round(x)

# converts any variable into a positive or absolute value of itself
# result = abs(y) -4 becomes 4 if y was -4

# puts a value up to a power
# result = pow(4, 3) 4^3 yields 64

# assigns a variable with a maximum/minimum between inputted numbers
# result = max(x, y, z) yields 5 as maximum
# result = min(x, y, z) yeilds 3.4 as minimum
print(result)

